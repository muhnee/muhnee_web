export default interface TooltipProps {
  /**
   * the heading of the tooltip (give it a nice brief title)
   */
  title?: string | number;

  /**
   * Description of the tooltip, provide a detailed summary or the value of the Chart here
   */
  description?: string;
}
