import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles({
  root: { flex: 1 },
  monthControl: {
    display: "flex",
    alignItems: "center"
  },
  summaryChartRow: {
    display: "flex",
    flexWrap: "wrap"
  }
});

export default useStyles;
