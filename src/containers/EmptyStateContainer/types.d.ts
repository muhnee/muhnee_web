export interface EmptyStateContainerProps {
  /**
   * the title
   */
  title: string;

  /**
   * secondary text, give some descriptive action of what the user may need to do
   */
  caption: string;
}
